 import { NavLink, Outlet } from 'react-router-dom' import { useAuth } from '../contexts/AuthContext' export default function Layout() { const { profile, isAdmin, signOut } = useAuth() return ( <div className="app-shell"> <div className="zellige-rule" /> <header className="app-header"> <div className="app-header-inner"> <div className="brand">Viva<span>Stay</span></div> <nav className="nav-links"> {isAdmin ? ( <> <NavLink to="/" end>Vue globale</NavLink> <NavLink to="/admin/biens">Biens & propriétaires</NavLink> <NavLink to="/admin/import">Import mensuel</NavLink> </> ) : ( <NavLink to="/" end>Mon dashboard</NavLink> )} <NavLink to="/mot-de-passe">Mot de passe</NavLink> </nav> <div className="header-right"> <span>{profile?.full_name || profile?.email}</span> <button onClick={signOut}>Déconnexion</button> </div> </div> </header> <main className="app-main"> <Outlet /> </main> </div> ) }
